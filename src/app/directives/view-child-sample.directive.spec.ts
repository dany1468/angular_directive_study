import { ViewChildSampleDirective } from './view-child-sample.directive';

describe('ViewChildSampleDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewChildSampleDirective();
    expect(directive).toBeTruthy();
  });
});
