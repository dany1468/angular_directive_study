import { ChangeColor2Directive } from './change-color2.directive';

describe('ChangeColor2Directive', () => {
  it('should create an instance', () => {
    const directive = new ChangeColor2Directive();
    expect(directive).toBeTruthy();
  });
});
