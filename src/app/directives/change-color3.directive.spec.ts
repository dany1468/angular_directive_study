import { ChangeColor3Directive } from './change-color3.directive';

describe('ChangeColor3Directive', () => {
  it('should create an instance', () => {
    const directive = new ChangeColor3Directive();
    expect(directive).toBeTruthy();
  });
});
