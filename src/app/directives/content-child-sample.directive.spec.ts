import { ContentChildSampleDirective } from './content-child-sample.directive';

describe('ContentChildSampleDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentChildSampleDirective();
    expect(directive).toBeTruthy();
  });
});
